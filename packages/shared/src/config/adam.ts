import compatibilityConfig from "./adam-compatibility.json" with { type: "json" };

export const adamCompatibilityTenantId = compatibilityConfig.compatibilityTenantId;

export const adamCompatibilityConfig = compatibilityConfig;
