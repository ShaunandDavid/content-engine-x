import compatibilityConfig from "./enoch-compatibility.json" with { type: "json" };

export const enochCompatibilityTenantId = compatibilityConfig.compatibilityTenantId;

export const enochCompatibilityConfig = compatibilityConfig;
